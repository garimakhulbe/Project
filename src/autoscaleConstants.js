exports = module.exports;

var autoscaleConstants = {
    TIMEOUT: 120,
    STORAGE_READ_INTERVAL: 10,
    STORAGE_WRITE_INTERVAL: 20,
    CHECK_STATUS_INTERVAL: 60

};

module.exports = autoscaleConstants;
